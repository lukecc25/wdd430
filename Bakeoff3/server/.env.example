PORT=4000

# Firebase Admin only (server). No Clerk. Never put FIREBASE_PRIVATE_KEY in client/.env.
# Project: cookquest-3b152
# Console → Project settings → Service accounts → Generate new private key (JSON)
# Copy project_id, client_email, and private_key from that file:

FIREBASE_PROJECT_ID=cookquest-3b152
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@cookquest-3b152.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
